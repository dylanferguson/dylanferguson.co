{
  "name": "dylanferguson.co",
  "install": "curl -fsSL https://mise.run | MISE_QUIET=1 sh && ~/.local/bin/mise trust && ~/.local/bin/mise install && ~/.local/bin/mise exec -- pnpm install --frozen-lockfile",
  "terminals": [
    { "name": "dev", "command": "~/.local/bin/mise exec -- pnpm dev --host" }
  ],
  "ports": [
    { "name": "web", "port": 4321 }
  ]
}
